{
  "//": [
    "PxD Room Configuration — Starter Template (v2)",
    "Copy this file to rooms/<game>/pxd/room.json and fill in your values.",
    "Lines starting with '//' are comments and must be removed from your copy",
    "(standard JSON does not allow comments).",
    "",
    "Required fields are marked REQUIRED. All others have usable defaults.",
    "Full reference: docs/ROOMS.md (fields), docs/PANES.md (pane library),",
    "docs/THEMING.md (theme tokens), docs/WIDGETS.md (widget authoring)."
  ],

  "//pxdVersion": "REQUIRED — do not change",
  "pxdVersion": "2",

  "//title": "REQUIRED — shown in the browser tab, page header, landing page",
  "title": "My Room Name",

  "//topicRoot": "REQUIRED — all default MQTT topics derive from this prefix",
  "topicRoot": "paradox/myroom",

  "//mqtt": "Connection settings. 'auto' is correct for nearly all deployments.",
  "mqtt": {
    "//broker": "'auto' = window.location.hostname (same Pi as the page)",
    "broker": "auto",
    "//port": "'auto' = page port (80 for HTTP, 443 for HTTPS)",
    "port": "auto",
    "//wsPath": "Nginx WebSocket proxy path — matches the /mqtt location block",
    "wsPath": "/mqtt",
    "//clientIdPrefix": "Optional. Random suffix is appended automatically.",
    "clientIdPrefix": "pxd_"
  },

  "//theme": [
    "Named theme + optional per-token overrides + optional custom fonts.",
    "See docs/THEMING.md for the shipped theme catalog and full token table."
  ],
  "theme": {
    "base": "midnight-teal",
    "overrides": {},
    "fonts": []
  },

  "//media": "Paths are relative to the packager output root.",
  "media": {
    "//hero": "Wide banner image, referenced from a 'content' pane (see below). Landscape, ~1200x300 px.",
    "hero": "media/hero.jpg",
    "//favicon": "Browser tab icon. 32x32 .ico or .png.",
    "favicon": "media/favicon.ico"
  },

  "//gameControl": [
    "Game-control pane settings. All fields are optional;",
    "defaults derive from topicRoot. See docs/ROOMS.md for full reference."
  ],
  "gameControl": {
    "//commandTopic": "Default: topicRoot/commands",
    "//stateTopic":   "Default: topicRoot/state",
    "//configTopic":  "Default: topicRoot/config",
    "//checklistStateTopic": "Default: topicRoot/checklist/state",
    "//heartbeatTimeoutMs":  "Default: 3000",
    "//emergencyActions": [
      "Array of quick-action buttons shown in the emergency modal.",
      "Each entry: { 'label': 'Unlock Door', 'command': 'unlock', 'param': 'front' }"
    ],
    "emergencyActions": []
  },

  "//timeLights": [
    "Time-lights pane settings. All fields are optional.",
    "See docs/ROOMS.md for full reference."
  ],
  "timeLights": {
    "//lightsStateTopic":     "Default: topicRoot/lights/state",
    "//lightsScenesTopicRoot": "If set, subscribes to <value>/scenes for a dynamic scene list",
    "//clockStateTopic":      "Default: topicRoot/clock/state",
    "//tvStateTopic":         "Default: topicRoot/tv/state"
  },

  "//hints": "Hints pane settings.",
  "hints": {
    "//hintTopic": "Default: topicRoot/hints"
  },

  "//system": "System pane settings.",
  "system": {
    "//warningTopics": [
      "MQTT topics whose messages appear in the warning log.",
      "Supports wildcards: paradox/myroom/+/warnings"
    ],
    "warningTopics": [
      "paradox/myroom/warnings"
    ],
    "//watchZones": [
      "One entry per service / device you want to monitor.",
      "A zone goes to 'Disconnected' after timeoutMs ms without a message."
    ],
    "watchZones": [
      { "id": "pfx",       "label": "PFx",       "topic": "paradox/myroom/+/state", "timeoutMs": 12000 },
      { "id": "wallclock", "label": "Wallclock",  "topic": "paradox/myroom/clock/state", "timeoutMs": 15000 }
    ]
  },

  "//sites": [
    "REQUIRED — one or more deployable UI variants. Each 'pxd' site has one or",
    "more pages, each with an ordered list of panes. See docs/PANES.md for the",
    "full pane library (game-control, time-lights, hints, system, widget-grid,",
    "camera-view, content, nav, divider) and docs/ROOMS.md for the full",
    "site/page schema (header/footer, description, external/manual sites).",
    "",
    "This starter defines a single 'control' site with the four core panes.",
    "Add a camera-view pane (docs/PANES.md § camera-view) or a widget-grid",
    "pane (docs/WIDGETS.md) as needed — consider splitting into two sites",
    "(e.g. 'simple' and 'live') if you want a camera-free variant too."
  ],
  "sites": [
    {
      "id": "control",
      "title": "Control",
      "description": "Core operator dashboard",
      "type": "pxd",
      "pages": [
        {
          "id": "main",
          "title": "My Room Name",
          "panes": [
            { "type": "content", "width": "full", "config": {
                "html": "<img class=\"pxd-hero-banner\" src=\"media/hero.jpg\" alt=\"My Room Name\">"
              } },
            { "type": "game-control", "width": "full", "config": {} },
            { "type": "time-lights", "width": "half", "config": {} },
            { "type": "hints", "width": "half", "config": {} },
            { "type": "system", "width": "full", "config": {} }
          ]
        }
      ]
    }
  ]
}
